# 480. Sliding Window Median

本题要求在数组 `nums` 上做长度为 `k` 的滑动窗口，返回每个窗口的中位数。  

难点：窗口移动时 **删除任意元素**（不是堆顶），但堆结构不支持 O(log n) 删除任意位置 → 需要使用“双堆 + 延迟删除（lazy deletion）”。

重点： 每一次add / remvoe 都要balance 一次

### 🧠 核心思想（High-Level Overview）
- 使用 **最大堆 leftHalf** 存储窗口较小的一半数字（堆顶最大）
- 使用 **最小堆 rightHalf** 存储窗口较大的一半数字（堆顶最小）
- 确保： 
    - leftHalfSize == rightHalfSize （窗口大小为偶数）
    - leftHalfSize == rightHalfSize + 1 （窗口大小为奇数）

- 删除旧数字时不能直接从堆删除 → 采用 **延迟删除（delayed map）**  
当某个“需要删除”的数字出现在堆顶时，再真正移除。

#### 🔧 步骤一：数据结构准备
- `leftHalf`：最大堆（PriorityQueue reverseOrder）
- `rightHalf`：最小堆
- `delayed`：HashMap，记录“标记删除但尚未从堆移除”的数字
- `leftHalfSize / rightHalfSize`：分别记录两个堆中“有效数字”的数量

> 注意：堆本体里的数字数量不代表有效数量  
> 有效数量 = 原数量 - 延迟删除的数量


#### 🔧 步骤二：加入一个数字 addNum(num)
1. 判断数字放入哪个堆
    - if (num <= leftHalf.peek()) → 加入左堆 else → 加入右堆

2. 对应堆的 size 计数 +1  
3. 调用 `balanceHeaps()` 保持两个堆平衡

### 🔧 步骤三：删除一个数字 removeNum(num)
因为删除任意元素不可能在 O(log n) 里做到，所以：
1. 在 delayed map 中标记该数字需要被删除：
2. 判断该数字属于哪个堆：
    - if (num <= leftHalf.peek()) → 左堆 size-- else → 右堆 size--
3. 若该数字恰好在堆顶 → 立即 prune 那个堆（真正删除）
4. 调用 `balanceHeaps()`

> 由于 prune 清理了顶端过期值，balanceHeaps() 时堆顶一定是有效的。


### 步骤四：平衡两个堆 balanceHeaps()
维持堆数量关系：

###  步骤五：真正删除堆顶的过期值 prune(heap)
不断移除 heap 顶部的“已被 delayed 标记的元素”：

> prune 的作用：  
> - balanceHeaps 会 poll() 堆顶  
> - poll 后暴露出的新堆顶可能是过期值  
> - prune 负责清理这些“新暴露的过期值”

```java
class Solution {
    private PriorityQueue<Integer> leftHalf;
    private PriorityQueue<Integer> rightHalf;
    private Map<Integer, Integer> delayed;
    private int leftHalfSize;
    private int rightHalfSize;

    public double[] medianSlidingWindow(int[] nums, int k) {
        leftHalf = new PriorityQueue<>(Collections.reverseOrder());
        rightHalf = new PriorityQueue<>();
        delayed = new HashMap<>(); // <number to be deleted, times>
        int n = nums.length;
        double[] res = new double[n-k+1];
        leftHalfSize = 0;
        rightHalfSize = 0;

        // 1. 初始化前 k 个元素
        for (int i = 0; i < k; i++) {
            addNum(nums[i]);
        } 

        res[0] = getMedian(k);

        // 2. 滑动窗口
        for (int i = k; i < n; i++) {
            int newNum = nums[i]; // 进入窗口的新元素
            int oldNum = nums[i-k]; // 离开窗口的旧元素
             
            addNum(newNum);
            removeNum(oldNum);  // 延迟删除

            res[i-k+1] = getMedian(k);
        }
        return res;
    }

    // 插入元素
    private void addNum(int num) {
        if (leftHalf.isEmpty() || num <= leftHalf.peek()) {
            leftHalf.offer(num);
            leftHalfSize++;
        } else {
            rightHalf.offer(num);
            rightHalfSize++;
        }
        balanceHeaps(); // balance after adding 
    }

    // 平衡两个堆 (保证 maxHeapSize >= minHeapSize，且差值不超过 1)
    private void balanceHeaps() {
        if (leftHalfSize > rightHalfSize + 1) {
            rightHalf.offer(leftHalf.poll());
            leftHalfSize--;
            rightHalfSize++;
            prune(leftHalf);
        } else if (leftHalfSize < rightHalfSize) {
            leftHalf.offer(rightHalf.poll());
            leftHalfSize++;
            rightHalfSize--;
            prune(rightHalf);
        }
    }
    
     // 延迟删除元素
    private void prune(PriorityQueue<Integer> heap) {
        while (!heap.isEmpty() && delayed.containsKey(heap.peek())) {
            int num = heap.peek();
            delayed.put(num, delayed.get(num)-1);
            if (delayed.get(num) == 0) delayed.remove(num);
            heap.poll();
        }
    }

    // 清理堆顶的过期元素
    private void removeNum(int num) {
        delayed.put(num, delayed.getOrDefault(num, 0)+1);
        
        if (num <= leftHalf.peek()) {
            leftHalfSize--;
            if (num == leftHalf.peek()) prune(leftHalf);
        } else {
            rightHalfSize--;
            if (num == rightHalf.peek()) prune(rightHalf);
        }
        balanceHeaps(); // balance after deleting
    }

    private double getMedian(int k) {
        if (k % 2 ==1) {
            return (double)leftHalf.peek();
        }
        return ((double)leftHalf.peek() + rightHalf.peek())/2.0;
    }
}

```

# LeetCode 480 — Sliding Window Median  
## 时间复杂度与空间复杂度分析

---

# 🕒 时间复杂度分析（Time Complexity）

## 核心操作的复杂度

算法中每次移动窗口（共 n 次），我们执行两个关键操作：

### **1. addNum(num)**  
- 将 num 插入最大堆或最小堆  
- 插入操作：`O(log k)`  
- 调用 `balanceHeaps()`：最多进行一次 poll + offer：  
    - 每个 poll 或 offer：`O(log k)`  
- prune() 清理堆顶：摊还复杂度（amortized）为 `O(1)`

**总体：`O(log k)`**

---

### **2. removeNum(num)**  
由于无法直接删除堆中的任意元素 → 使用“延迟删除”：

步骤包含：
- 在 delayed map 中标记删除：`O(1)`
- 可能 prune 堆顶：摊还 `O(1)`
- 调用 `balanceHeaps()`：再次最多 poll + offer，`O(log k)`

**总体：`O(log k)`**

---

### **3. getMedian()**
- 读取 leftHalf.peek() / rightHalf.peek() → `O(1)`

---

## 🧮 对整条数组的操作
滑动窗口从左到右，总共会执行 `n` 次 addNum 和 removeNum。

因此总时间复杂度：

# ⭐ **总时间复杂度 = O(n log k)**

这是当前题目的最优复杂度（所有方法都做不到更好）。

---

# 🧊 空间复杂度分析（Space Complexity）

空间来源主要有三部分：

### **1. 两个堆：leftHalf（max-heap）与 rightHalf（min-heap）**

- 每个堆最多存 k 个元素  
- 尽管堆中可能有“过期元素”，但总不超过窗口大小

